# Created by Tom R.

# Top system information commands

(echo "uname -a:" && uname -a && echo && echo "/etc/os-release:" && cat /etc/os-release && echo && echo "lsb_release -a:" && lsb_release -a 2>/dev/null && echo && echo "hostnamectl:" && hostnamectl)
